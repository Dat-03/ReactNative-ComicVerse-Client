import {ReactElement} from 'react';
import {ViewProps} from 'react-native';

export type TextCustomProps = {
  textBold?: boolean;
  textLight?: boolean;
  textPrimary?: boolean;
  title?: string;
  number?: number;
};
