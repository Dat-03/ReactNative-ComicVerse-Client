import {ReactElement} from 'react';

import {TextStyle, ViewProps} from 'react-native';

export type AuthProps = {
  title?: string;
  titleIcon?: boolean;
  subTitle?: string;
  avatar?: ReactElement;
};
